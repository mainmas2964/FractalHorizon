#version 430 core

layout(location = 0) in vec3 aPos;
layout(location = 1) in vec2 aUV;
layout(location = 2) in uint aLight;

uniform mat4 uMVP;

out vec2 vUV;
flat out uint vLight;

void main() {
    vUV = aUV;
    vLight = aLight;
    gl_Position = uMVP * vec4(aPos, 1.0);
}
