#version 430 core

in vec3 vNormal;
in vec2 vUV;

out vec4 FragColor;

void main() {
    // map normal from [-1,1] to [0,1]
    vec3 n = normalize(vNormal) * 0.5 + 0.5;
    FragColor = vec4(n, 1.0);
}
