#version 430 core

in vec2 vUV;
flat in uint vLight;

out vec4 FragColor;

uniform vec3 u_color;

void main() {
    // debug: tint by UV and a constant color, optionally mod by light
    float l = clamp(float(vLight) / 255.0, 0.2, 1.0);
    vec3 shaded = u_color * l;
    FragColor = vec4(shaded, 1.0);
